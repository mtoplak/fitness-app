name: CI/CD Pipeline

on:
  push:
    branches: [ main, pre-production, production ]
  pull_request:
    branches: [ main, pre-production, production ]

jobs:
  # Frontend Testing Job
  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
      
      - name: Run tests with coverage
        working-directory: ./frontend
        run: npm run test:coverage
      
      - name: Upload frontend coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage-report
          path: frontend/coverage/
          retention-days: 30
      
      - name: Display coverage summary
        working-directory: ./frontend
        run: |
          echo "## 📊 Frontend Test Coverage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f coverage/coverage-summary.json ]; then
            echo "| Metric | Coverage |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|----------|" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 📝 Lines | '+d.lines.pct+'% ('+d.lines.covered+'/'+d.lines.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 📋 Statements | '+d.statements.pct+'% ('+d.statements.covered+'/'+d.statements.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 🔀 Branches | '+d.branches.pct+'% ('+d.branches.covered+'/'+d.branches.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| ⚡ Functions | '+d.functions.pct+'% ('+d.functions.covered+'/'+d.functions.total+') |'" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Coverage summary not generated." >> $GITHUB_STEP_SUMMARY
          fi

  # Backend Testing Job
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand({ping: 1})'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json
      
      - name: Install dependencies
        working-directory: ./server
        run: npm ci
      
      - name: Run tests with coverage
        working-directory: ./server
        env:
          MONGODB_URI: mongodb://localhost:27017/fitness-app-test
          JWT_SECRET: test-secret-key-for-ci
          NODE_ENV: test
        run: npm test
      
      - name: Upload backend coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage-report
          path: server/coverage/
          retention-days: 30
      
      - name: Display coverage summary
        working-directory: ./server
        run: |
          echo "## 📊 Backend Test Coverage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f coverage/coverage-summary.json ]; then
            echo "| Metric | Coverage |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|----------|" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 📝 Lines | '+d.lines.pct+'% ('+d.lines.covered+'/'+d.lines.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 📋 Statements | '+d.statements.pct+'% ('+d.statements.covered+'/'+d.statements.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| 🔀 Branches | '+d.branches.pct+'% ('+d.branches.covered+'/'+d.branches.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/coverage-summary.json').total; '| ⚡ Functions | '+d.functions.pct+'% ('+d.functions.covered+'/'+d.functions.total+') |'" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Coverage summary not generated." >> $GITHUB_STEP_SUMMARY
          fi

  # Combined Coverage Report Job
  coverage-report:
    name: Combined Coverage Report
    needs: [frontend-tests, backend-tests]
    runs-on: ubuntu-latest
    
    steps:
      - name: Download frontend coverage
        uses: actions/download-artifact@v4
        with:
          name: frontend-coverage-report
          path: ./coverage/frontend
      
      - name: Download backend coverage
        uses: actions/download-artifact@v4
        with:
          name: backend-coverage-report
          path: ./coverage/backend
      
      - name: Display combined coverage info
        run: |
          echo "# 🎯 Combined Test Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 🎨 Frontend Coverage" >> $GITHUB_STEP_SUMMARY
          if [ -f ./coverage/frontend/coverage-summary.json ]; then
            echo "| Metric | Coverage |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|----------|" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/frontend/coverage-summary.json').total; '| 📝 Lines | '+d.lines.pct+'% ('+d.lines.covered+'/'+d.lines.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/frontend/coverage-summary.json').total; '| 📋 Statements | '+d.statements.pct+'% ('+d.statements.covered+'/'+d.statements.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/frontend/coverage-summary.json').total; '| 🔀 Branches | '+d.branches.pct+'% ('+d.branches.covered+'/'+d.branches.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/frontend/coverage-summary.json').total; '| ⚡ Functions | '+d.functions.pct+'% ('+d.functions.covered+'/'+d.functions.total+') |'" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Coverage data not available" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## ⚙️ Backend Coverage" >> $GITHUB_STEP_SUMMARY
          if [ -f ./coverage/backend/coverage-summary.json ]; then
            echo "| Metric | Coverage |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|----------|" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/backend/coverage-summary.json').total; '| 📝 Lines | '+d.lines.pct+'% ('+d.lines.covered+'/'+d.lines.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/backend/coverage-summary.json').total; '| 📋 Statements | '+d.statements.pct+'% ('+d.statements.covered+'/'+d.statements.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/backend/coverage-summary.json').total; '| 🔀 Branches | '+d.branches.pct+'% ('+d.branches.covered+'/'+d.branches.total+') |'" >> $GITHUB_STEP_SUMMARY
            node -pe "const d=require('./coverage/backend/coverage-summary.json').total; '| ⚡ Functions | '+d.functions.pct+'% ('+d.functions.covered+'/'+d.functions.total+') |'" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Coverage data not available" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📁 **Detailed HTML reports** are available as artifacts in the Actions tab above." >> $GITHUB_STEP_SUMMARY
          echo "💾 **Download artifacts:** \`frontend-coverage-report\` and \`backend-coverage-report\`" >> $GITHUB_STEP_SUMMARY

  sonarcloud:
    name: SonarCloud Analysis
    needs: [frontend-tests, backend-tests]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Download frontend coverage
        uses: actions/download-artifact@v4
        with:
          name: frontend-coverage-report
          path: ./frontend/coverage
      
      - name: Download backend coverage
        uses: actions/download-artifact@v4
        with:
          name: backend-coverage-report
          path: ./server/coverage
      
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      
      - name: SonarCloud Analysis Summary
        run: |
          echo "## 🔍 SonarCloud Analysis" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Code analysis completed successfully." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [View detailed results on SonarCloud](https://sonarcloud.io/project/overview?id=${{ secrets.SONAR_PROJECT_KEY }})" >> $GITHUB_STEP_SUMMARY

  quality-gate:
    name: Quality Gate Check
    needs: sonarcloud
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/production' || github.ref == 'refs/heads/pre-production'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Wait for Quality Gate
        run: |
          echo "⏳ Waiting for Quality Gate result..."
          sleep 10
      
      - name: Check Quality Gate Status
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: false
      
      - name: Quality Gate Success
        if: success()
        run: |
          echo "## ✅ Quality Gate - SUCCESS" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All quality standards passed." >> $GITHUB_STEP_SUMMARY
          echo "Deployment may proceed." >> $GITHUB_STEP_SUMMARY
      
      - name: Quality Gate Failed
        if: failure()
        run: |
          echo "## ❌ Quality Gate - FAILED" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ Quality gates were not met!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Deployment is blocked. Please fix issues in SonarCloud." >> $GITHUB_STEP_SUMMARY
          exit 1
